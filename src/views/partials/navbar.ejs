<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
  <div class="container">
    <a class="navbar-brand" href="/"><i class="bi bi-house-door-fill me-2"></i>Classroom Manager</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link <%= locals.currentPath && currentPath.startsWith('/class') ? 'active' : '' %>" href="/class"><i class="bi bi-collection-fill me-1"></i>My Classes</a>
          </li>
          <% if (user.role === 'teacher') { %>
            <li class="nav-item">
              <a class="nav-link <%= locals.currentPath && currentPath === '/class/create' ? 'active' : '' %>" href="/class/create"><i class="bi bi-plus-circle-fill me-1"></i>Create Class</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link <%= locals.currentPath && currentPath === '/class/join' ? 'active' : '' %>" href="/class/join"><i class="bi bi-person-plus-fill me-1"></i>Join Class</a>
            </li>
          <% } %>
        <% } %>
      </ul>
      <ul class="navbar-nav">
        <% if (user) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle <%= locals.currentPath && currentPath.startsWith('/profile') ? 'active' : '' %>" href="#" id="navbarDropdownUser" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if (user.profileImage) { %>
                <img src="<%= user.profileImage %>" alt="<%= user.username %>" class="rounded-circle me-1" style="width: 24px; height: 24px; object-fit: cover;">
              <% } else { %>
                <i class="bi bi-person-circle me-1"></i>
              <% } %>
              <%= user.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownUser">
              <li><a class="dropdown-item <%= locals.currentPath && currentPath === '/profile' ? 'active' : '' %>" href="/profile"><i class="bi bi-person-badge-fill me-2"></i>View Profile</a></li>
              <li><a class="dropdown-item <%= locals.currentPath && currentPath === '/profile/edit' ? 'active' : '' %>" href="/profile/edit"><i class="bi bi-pencil-square me-2"></i>Edit Profile</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link <%= locals.currentPath && currentPath === '/auth/login' ? 'active' : '' %>" href="/auth/login"><i class="bi bi-box-arrow-in-right me-1"></i>Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= locals.currentPath && currentPath === '/auth/register' ? 'active' : '' %>" href="/auth/register"><i class="bi bi-person-plus-fill me-1"></i>Register</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
